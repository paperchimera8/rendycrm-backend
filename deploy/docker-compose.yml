services:
  api:
    build:
      context: ..
      dockerfile: Dockerfile
    ports:
      - "${API_PORT:-3000}:3000"
    environment:
      PORT: "3000"
      STATIC_DIR: "/web"
      BOT_RUNTIME_BASE_URL: "http://bot-ts:3100"
      BOT_RUNTIME_INTERNAL_TOKEN: ${BOT_RUNTIME_INTERNAL_TOKEN:-change-me-in-production}
      POSTGRES_DSN: ${POSTGRES_DSN:-}
      POSTGRES_HOST: ${POSTGRES_HOST:-}
      POSTGRES_PORT: ${POSTGRES_PORT:-5432}
      POSTGRES_DB: ${POSTGRES_DB:-}
      POSTGRES_USER: ${POSTGRES_USER:-}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-}
      POSTGRES_SSLMODE: ${POSTGRES_SSLMODE:-prefer}
      POSTGRES_SSLROOTCERT: ${POSTGRES_SSLROOTCERT:-}
      POSTGRES_SSLROOTCERT_URL: ${POSTGRES_SSLROOTCERT_URL:-}
      REDIS_ADDR: ${REDIS_ADDR:-}
      REDIS_HOST: ${REDIS_HOST:-}
      REDIS_PORT: ${REDIS_PORT:-6379}
      REDIS_USERNAME: ${REDIS_USERNAME:-}
      REDIS_PASSWORD: ${REDIS_PASSWORD:-}
      REDIS_DB: ${REDIS_DB:-0}
      PUBLIC_BASE_URL: ${PUBLIC_BASE_URL:-http://localhost:3000/api}
      APP_ENCRYPTION_SECRET: ${APP_ENCRYPTION_SECRET:-change-me-in-production}
      ENABLE_DEMO_SEED: ${ENABLE_DEMO_SEED:-false}
      CORS_ALLOWED_ORIGINS: ${CORS_ALLOWED_ORIGINS:-}
    restart: unless-stopped
  bot-ts:
    build:
      context: ..
      dockerfile: apps/bot-ts/Dockerfile
    environment:
      PORT: "3100"
      GO_API_BASE_URL: "http://api:3000"
      BOT_RUNTIME_INTERNAL_TOKEN: ${BOT_RUNTIME_INTERNAL_TOKEN:-change-me-in-production}
    restart: unless-stopped
