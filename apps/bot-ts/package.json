{
  "name": "rendycrm-bot-ts",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run",
    "test:watch": "vitest",
    "check": "npm run build && npm run test"
  },
  "devDependencies": {
    "@types/node": "^24.5.2",
    "typescript": "^5.7.3",
    "vitest": "^3.2.4"
  }
}
